/**
 * Group Filter (ie: Employee, Workgroup, Department)
 * Use in Assigning To List
 */
const GroupFilter = Object.freeze({
  Employee: 'Employee',
  Workgroup: 'Workgroup',
  Department: 'Department',
});

export default GroupFilter;
